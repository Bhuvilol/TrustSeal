{
  "name": "trustseal-contract",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "compile": "hardhat compile",
    "deploy:amoy": "hardhat run scripts/deploy.js --network amoy"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^3.0.8",
    "dotenv": "^16.6.1",
    "ethers": "^6.15.0",
    "hardhat": "^2.24.3"
  }
}